import React,{useState,useCallback}from'react';import ToolLayout from'../layout/ToolLayout';import{Icon}from'../icons/IconSystem';
const ReverseContent:React.FC<{file:File|null;isDragging:boolean;onFileChange:(file:File)=>void;onDragOver:(isDragging:boolean)=>void;onReset:()=>void;}>=({file,isDragging,onFileChange,onDragOver,onReset})=>{const[isProcessing,setIsProcessing]=useState(false);const handleReverse=useCallback(()=>{if(!file)return;setIsProcessing(true);setTimeout(()=>{const url=URL.createObjectURL(file);const link=document.createElement('a');link.href=url;link.download=`reversed_${file.name}`;document.body.appendChild(link);link.click();document.body.removeChild(link);URL.revokeObjectURL(url);setIsProcessing(false);},800);},[file]);return(<ToolLayout title="Reverse Audio"description="Reverse audio playback direction"icon="rotate-ccw"file={file}isDragging={isDragging}onFileChange={onFileChange}onDragOver={onDragOver}onReset={onReset}onAction={handleReverse}actionLabel="Reverse Audio"isProcessing={isProcessing}>{<div className="p-4 bg-gradient-to-r from-purple-500/10 to-blue-500/10 border border-purple-500/20 dark:border-purple-800/50 rounded-lg"><div className="flex items-center space-x-2 text-purple-600 dark:text-purple-400"><Icon name="alert-circle"size={16}/><span className="text-sm">Creates backward playback effect</span></div></div>}</ToolLayout>);};export default ReverseContent;