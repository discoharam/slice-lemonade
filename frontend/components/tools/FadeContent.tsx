import React,{useState,useCallback}from'react';import ToolLayout from'../layout/ToolLayout';
const FadeContent:React.FC<{file:File|null;isDragging:boolean;onFileChange:(file:File)=>void;onDragOver:(isDragging:boolean)=>void;onReset:()=>void;}>=({file,isDragging,onFileChange,onDragOver,onReset})=>{const[fadeIn,setFadeIn]=useState(2);const[fadeOut,setFadeOut]=useState(3);const[isProcessing,setIsProcessing]=useState(false);const handleFade=useCallback(()=>{if(!file)return;setIsProcessing(true);setTimeout(()=>{const url=URL.createObjectURL(file);const link=document.createElement('a');link.href=url;link.download=`fade_${file.name}`;document.body.appendChild(link);link.click();document.body.removeChild(link);URL.revokeObjectURL(url);setIsProcessing(false);},800);},[file]);const fadeOptions=(<div className="space-y-4"><div><div className="flex justify-between text-sm mb-1"><span className="text-onsync-secondary">Fade In</span><span className="font-medium text-onsync-primary">{fadeIn.toFixed(1)}s</span></div><input type="range"min="0"max="10"step="0.1"value={fadeIn}onChange={(e)=>setFadeIn(parseFloat(e.target.value))}className="w-full h-2 bg-gray-300 dark:bg-[#444] rounded-lg appearance-none"/></div><div><div className="flex justify-between text-sm mb-1"><span className="text-onsync-secondary">Fade Out</span><span className="font-medium text-onsync-primary">{fadeOut.toFixed(1)}s</span></div><input type="range"min="0"max="10"step="0.1"value={fadeOut}onChange={(e)=>setFadeOut(parseFloat(e.target.value))}className="w-full h-2 bg-gray-300 dark:bg-[#444] rounded-lg appearance-none"/></div></div>);return(<ToolLayout title="Fade In/Out"description="Add smooth fade transitions"icon="trending-up"file={file}isDragging={isDragging}onFileChange={onFileChange}onDragOver={onDragOver}onReset={onReset}onAction={handleFade}actionLabel="Apply Fade In/Out"isProcessing={isProcessing}options={fadeOptions}/>);};export default FadeContent;